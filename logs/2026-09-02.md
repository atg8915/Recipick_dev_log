# 2026-09-02 개발 기록 (atg8915)

## 백엔드
### 기능
- 캘린더 식단 저장(드래그 앤 드롭) API 추가: `CalendarRestController`에 `POST /calendar/item` 신설, `CalendarServiceImpl`/`CalendarMapper`에 `upsertCalendarItem` 추가. Oracle `MERGE` 문으로 동일 (user_id, meal_date, meal_type) 조합이면 UPDATE, 없으면 INSERT 처리.
  - 병합 커밋(4b3622d)이라 관리자 관련 매퍼/컨트롤러(`AdminMapper`, `AdminServiceImpl`, `AdminRestController`) 변경도 함께 포함됨.

```java
// CalendarRestController.java (커밋: 4b3622d)
@PostMapping("/calendar/item")
public ResponseEntity<String> calendar_item_upsert(@RequestBody CalendarItemVO vo) {
    try {
        cService.upsertCalendarItem(vo);
    } catch (Exception ex) {
        ex.printStackTrace();
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).build();
    }
    return ResponseEntity.ok("OK");
}
```

```xml
<!-- calendarItem-mapper.xml (커밋: 4b3622d) -->
<insert id="upsertCalendarItem" parameterType="com.sist.web.vo.CalendarItemVO">
  MERGE INTO CALENDAR_ITEM ci
  USING (SELECT #{user_id} AS user_id, #{meal_date} AS meal_date, #{meal_type} AS meal_type FROM dual) src
  ON (ci.user_id = src.user_id AND ci.meal_date = src.meal_date AND ci.meal_type = src.meal_type)
  WHEN MATCHED THEN
      UPDATE SET ci.RCP_SEQ = #{rcp_seq}
  WHEN NOT MATCHED THEN
      INSERT (id, user_id, meal_date, meal_type, RCP_SEQ)
      VALUES (seq_calendar_item.NEXTVAL, #{user_id}, #{meal_date}, #{meal_type}, #{rcp_seq})
</insert>
```

## 프론트엔드
### 기능
- 캘린더 드래그앤드롭 배치 마무리: `MealPlanView.vue`에서 `RecipeSearchPanel` import 경로 대소문자 불일치(`Recipesearchpanel.vue` → `RecipeSearchPanel.vue`) 수정.

### 수정
- `RecipeSearchPanel.vue` 주석 보완 (동작 설명 주석 추가, 로직 변경 없음)
- dev 브랜치 병합 충돌 해결 (33개 파일, PR #4 반영 — 냉장고/관리자/레시피/로그인/회원가입 등 다수 뷰·컴포넌트 병합)

### 리팩터
- 캘린더 관련 파일 폴더 재배치: `RecipeSearchPanel.vue`를 `recipe` → `calendar` 폴더로, `MealPlanView.vue`를 `views` → `views/calendar`로 이동. 라우터 경로만 갱신, 코드 로직 변경 없음.

### 이슈 / 해결
- [파일명 대소문자 불일치로 인한 import 경로 오류 가능성]
- 원인: `RecipeSearchPanel.vue` import 시 실제 파일명(`Recipesearchpanel.vue`)과 대소문자가 달라 대소문자 구분 파일시스템(리눅스 빌드 등)에서 모듈 해석 실패 우려
- 해결:
  ```js
  // MealPlanView.vue (커밋: 3ffe549)
  - import RecipeSearchPanel from '@/components/recipe/Recipesearchpanel.vue'
  + import RecipeSearchPanel from '@/components/recipe/RecipeSearchPanel.vue'
  ```

- [dev 브랜치 병합 충돌]
- 원인: dev 브랜치 병합 시 냉장고/관리자/레시피 관련 다수 파일에서 충돌 발생 (커밋 메시지에 상세 에러 내용은 없음)
- 해결: 병합 충돌 해결 커밋(518037f)으로 정리, 33개 파일 반영

## 다음 할 일
- (커밋 메시지 기준으로 명시된 다음 작업 없음)
