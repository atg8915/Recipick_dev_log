# Recipick 개발일지 자동화

이 저장소는 Recipick 프로젝트(한식 레시피 사이트)의 개발 진행 기록을 남기기 위한 저장소입니다.
건희님 본인이 작성한 커밋만 골라 백엔드/프론트엔드를 통합해서 기록합니다.

## 참조 저장소 경로
- 백엔드: C:\springBootDev\bootStudy\Recipick
- 프론트엔드: C:\FinalProject\Recipick-ui

## 커밋 메시지 규칙
- 형식: `atg8915 타입 : 내용`
- 타입 6종: 기능 / 수정 / 스타일 / 리팩터 / 문서 / 설정
- 본인 아이디(atg8915)로 시작하는 커밋만 개발일지에 포함

## /daily-dev-log 커맨드

사용자가 "오늘 개발 기록 정리해줘" 또는 `/daily-dev-log`를 요청하면 다음을 수행한다:

1. 백엔드 저장소에서 오늘 날짜, 본인(atg8915) 커밋만 조회
   `git -C "C:\springBootDev\bootStudy\Recipick" log --grep="^atg8915" --since="today 00:00:00" --pretty=format:"%h %s" --stat`

2. 프론트엔드 저장소에서 오늘 날짜, 본인(atg8915) 커밋만 조회
   `git -C "C:\FinalProject\Recipick-ui" log --grep="^atg8915" --since="today 00:00:00" --pretty=format:"%h %s" --stat`

3. 각 커밋 메시지를 타입별로 분류: 기능 / 수정 / 스타일 / 리팩터 / 문서 / 설정

4. 커밋 diff(--stat 및 필요시 상세 diff)를 참고해 각 항목을 다음 관점으로 정리:
   - 무엇을 구현했는지 (기능 단위로 요약)
   - 어떤 이슈를 겪었고 어떻게 해결했는지 (에러 메시지, 원인, 해결 방법)
   - 남은 문제나 다음에 이어서 할 작업 (있다면)

5. 코드 스니펫 포함 기준:
   - 이슈를 해결한 핵심 로직이거나, 구조적으로 의미 있는 설계 결정을 보여주는 코드만 발췌
   - 전체 diff를 그대로 넣지 않고, 핵심 부분만 5~15줄 내외로 축약
   - 단순 스타일 수정, 변수명 변경 등 사소한 변경에는 코드 스니펫을 넣지 않음
   - 스니펫에는 어떤 파일인지, 관련 커밋 해시를 함께 표기

6. 아래 포맷으로 `logs\YYYY-MM-DD.md` 파일 생성:

​```markdown
# YYYY-MM-DD 개발 기록 (atg8915)

## 백엔드
### 기능
- ...
### 수정
- ...
### 스타일
- ...
### 리팩터
- ...
### 문서
- ...
### 설정
- ...
### 이슈 / 해결
- [문제 상황]
- 원인: ...
- 해결:
  ​```java
  // 핵심 코드 5~15줄
  ​```
  (파일: ..., 커밋: ...)

## 프론트엔드
(위와 동일한 구조)

## 다음 할 일
- ...
​```

6. 해당 타입 커밋이 없으면 그 섹션은 생략한다 (빈 항목으로 채우지 않음).
7. 오늘 커밋이 백엔드/프론트 중 한쪽에 아예 없으면 "오늘 커밋 없음"으로 표기한다.
